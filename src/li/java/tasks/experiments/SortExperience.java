package li.java.tasks.experiments;

public class SortExperience {
	public static void main(String args[]) {
		int[] vals = { 1, 2, 323, 34627, 23, -4, 0, -345, 56, 27, 10, 235, -3, 3 };
		int temp;
		int min;
		int max;
		
		// for (int index = 0; index < vals.length-1; index++){
		// min = index;
		// for (int scan = index+1; scan < vals.length; scan++){
		// if (vals[scan] < vals[min])
		// min = scan;
		// }
		//
		// // Swap the values
		// temp = vals[min];
		// vals[min] = vals[index];
		// vals[index] = temp;
		// }

		// for (int i = vals.length-1; i >= 0; i--){
		// for(int j = 0; j < i; j++) {
		// if(vals[j] > vals[j+1]) {
		// temp = vals[j];
		// vals[j] = vals[j + 1];
		// vals[j + 1] = temp;
		// }
		// }
		// }

//		for (int j = 0; j < vals.length - 1; j++) {
//			for (int i = 0; i < vals.length - 1; i++) {
//				while (vals[i + 1] < vals[i]) {
//					temp = vals[i + 1];
//					vals[i + 1] = vals[i];
//					vals[i] = temp;
//				}
//			}
//		}

//		for (int x : vals) {
//			System.out.print(x + " ");
//		}
		
//		String str ="string";
//		String reversedStr = "";
//		for(int i = str.length() - 1; i >= 0 ; i--) {
//			reversedStr = reversedStr + str.charAt(i);
//		} 
//		System.out.println(reversedStr);
		
		
		
		
	}
}